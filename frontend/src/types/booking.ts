export interface BookingRisk { probability:number; level:'Low'|'Medium'|'High'; model:string; accuracy:number; f1:number; rocAuc:number; indicators:string[]; }
export interface BookingDetails { hotel:string; leadTime:number; arrivalMonth:string; weekendNights:number; weekNights:number; adults:number; children:number; babies:number; meal:string; country:string; marketSegment:string; distributionChannel:string; repeatedGuest:string; previousCancellations:number; previousBookings:number; roomType:string; depositType:string; customerType:string; adr:number; parkingSpaces:number; specialRequests:number; }
